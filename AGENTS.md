# 停车场智能网络照明感应系统 · 项目规格文档

> 本文档是项目的唯一事实来源（Single Source of Truth）。每次开发任务开始前，请先完整阅读本文档，理解项目定位、技术约束和业务流程，再动手写代码。如果某次任务对文档中的结构 / 流程 / 规范做出了调整，必须同步更新本文档对应章节，保证文档与代码库始终一致。

---

## 1. 项目定位

**项目名称**：停车场智能网络照明感应系统

**项目性质**：这是一个**纯前端演示系统**，**不接入真实后端和硬件**。设备、传感器、告警和能耗均由本地 Mock 层模拟；界面和操作流程应接近真实管理后台，但不得将模拟数据标称为真实采集数据。

**核心卖点**（写代码时始终围绕这几点展开）：
1. 车位/通道感应到车辆 → 联动灯组自动调光，无车持续一段时间后自动回到低亮度待机
2. 全场设备（灯具、传感器）可远程集中管理、手动干预
3. 分区/策略化管理，不同区域可配置不同的联动规则和定时策略
4. 故障与离线告警实时呈现
5. 能耗可视化，体现"智能调光比常亮节省用电"的核心价值主张

**目标用户**：物业管理人员 / 停车场运营方后台使用者。系统定位是**管理后台**，不是面向车主的小程序或 App。

---

## 2. 技术栈

| 类别 | 选型 | 备注 |
|---|---|---|
| 框架 | Vue 3 + Vite | 使用 `<script setup>` 组合式 API |
| 状态管理 | Pinia | 每个业务域一个 store，不要把所有状态塞进一个大 store |
| UI 组件库 | Element Plus | 后台管理类页面直接用现成组件，不重复造轮子 |
| 图表 | ECharts（通过 `vue-echarts` 封装） | 用于能耗趋势、告警频次统计 |
| 路由 | Vue Router 4 | 使用路由懒加载 |
| 车库平面图 | 原生 SVG（不引入额外地图库） | 点位用 `<circle>` / `<rect>`，颜色绑定灯具状态 |
| 运行数据 | 内存 Pinia store | 设备状态、告警、策略和能耗仅在当前页面会话有效，刷新后重置 |
| 演示账号 | `localStorage` | 仅保存本机注册账号以支持本地登录校验；不用于运行数据 |
| 代码规范 | ESLint + Prettier | 统一交给 lint 配置，不要手动纠结风格 |

**明确不做的事**：不接入真实 HTTP 后端、WebSocket 服务、硬件网关或地图 SDK；不做服务端鉴权、角色权限、找回密码和跨设备账号同步。本地注册/登录仅用于演示，不能作为生产环境安全机制，不应使用真实业务密码。

---

## 3. 目录结构

```
src/
├── mock/                      # 模拟数据与设备行为模拟器（系统的"假后端"，最核心的部分）
│   ├── devices.js             # 灯具/传感器设备初始数据
│   ├── zones.js                # 车库分区、楼层及 SVG 底图几何数据
│   ├── rules.js                 # 联动策略规则的初始数据
│   ├── simulator.js            # 定时器驱动的模拟引擎：触发感应、执行联动、注入故障
│   └── alerts.js               # 告警生成逻辑
│
├── store/
│   ├── deviceStore.js           # 设备（灯具 + 传感器）实时状态
│   ├── zoneStore.js              # 分区/车位状态
│   ├── ruleStore.js               # 联动策略配置
│   ├── alertStore.js             # 告警列表、未处理数、已处理状态
│   ├── energyStore.js            # 会话能耗累计与趋势采样
│   └── userStore.js              # 本地登录态，不含角色权限
│
├── views/
│   ├── Dashboard/                # 总览驾驶舱
│   ├── ZoneMonitor/              # 分区监控（平面图 + 实时点位状态）
│   ├── DeviceManage/             # 设备台账管理
│   ├── SceneControl/             # 联动策略与场景配置
│   ├── AlertCenter/               # 告警中心
│   ├── EnergyReport/              # 能耗统计报表
│   └── Login/
│   └── Register/
│
├── components/
│   ├── FloorMap/                  # 车库平面图组件
│   ├── DeviceCard/                # 设备状态卡片
│   ├── AlertItem/                 # 告警列表项
│   └── ChartPanel/                # 图表容器封装
│
├── router/
│   └── index.js
│
├── utils/
│   └── simulateSensor.js         # 感应触发 → 联动调光的核心算法，被 simulator.js 调用
│   └── energy.js                 # 按状态持续时间累计会话能耗
│   └── validateRule.js           # 联动策略字段与跨字段校验
│
└── App.vue
```

**约定**：
- 新增页面必须在 `views/` 下建独立文件夹，不要把多个页面塞进一个文件
- 设备、分区、策略和告警的初始数据必须能追溯到 `mock/`；会话能耗由这些运行状态计算；注册账号是唯一例外，来自浏览器 `localStorage`。禁止在组件里硬编码大段假数据
- 组件命名使用大驼峰，文件夹与主组件同名（如 `FloorMap/FloorMap.vue`）
- 设备管理与分区监控共用 `DeviceCard/DeviceCard.vue` 展示详情并执行灯具手动操作，统一调用模拟引擎；感应器详情不提供灯具控制
- 楼层 SVG 底图几何数据由 `mock/zones.js` 初始化进 `zoneStore.floorLayouts`，平面图组件只接收 store 提供的布局与实时设备点位

---

## 4. 核心数据模型

以下字段是当前阶段的基准结构，如果开发中需要扩展字段，请直接在本文档表格中追加行，保持文档与代码同步。

### 4.1 设备（Device）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 设备唯一 ID |
| type | 'light' \| 'sensor' | 设备类型：灯具 或 感应器 |
| zoneId | string | 所属分区 ID |
| status | 'online' \| 'offline' \| 'fault' | 设备在线状态 |
| lightState | 'off' \| 'standby' \| 'full' \| null | 仅灯具有效；`full` 表示工作模式，实际亮度以 `brightness` 为准 |
| brightness | number (0-100) \| null | 仅灯具有效；`off` 时为 0 |
| ratedPowerW | number \| null | 仅灯具有效；额定功率，单位 W，必须大于 0 |
| triggered | boolean \| null | 仅传感器有效：是否处于触发保持期 |
| triggeredUntilAt | timestamp \| null | 仅传感器有效：触发保持结束时间 |
| manualUntilAt | timestamp \| null | 仅灯具有效：手动模式到期时间；`null` 表示自动模式 |
| recoverAt | timestamp \| null | 离线或故障设备的模拟恢复时间；正常设备为 `null` |
| mapX | number | 设备点位在对应楼层 SVG `viewBox` 内的横坐标 |
| mapY | number | 设备点位在对应楼层 SVG `viewBox` 内的纵坐标 |
| lastActiveAt | timestamp | 最近一次状态变化时间 |

`timestamp` 统一为毫秒级 Unix 时间戳。非适用字段用 `null`。设备离线/故障时保留上次亮度供详情展示，但不参与联动；能耗估算按停止输出处理。恢复在线后立即按当前分区感应、策略和手动模式重新计算灯具状态。

### 4.2 分区（Zone）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 分区唯一 ID |
| name | string | 分区名称，如"B1-A区" |
| floor | string | 所属楼层 |
| deviceIds | string[] | 归属该分区的设备 ID 列表 |
| ruleId | string | 当前生效的联动策略 ID |
| delayUntilAt | timestamp \| null | 全部在线传感器结束触发后的回落截止时间；无倒计时时为 `null` |

### 4.3 联动策略（Rule）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 策略唯一 ID |
| name | string | 策略名称 |
| triggerHoldSeconds | number | 传感器每次触发后保持 `true` 的秒数，必须大于 0 |
| triggerDelayOff | number | 分区内全部在线传感器结束触发后，灯组回到待机的延时秒数，不小于 0 |
| standbyBrightness | number | 普通时段的待机亮度百分比 |
| fullBrightness | number | 感应后的工作亮度百分比 |
| scheduleEnabled | boolean | 是否启用定时待机亮度 |
| scheduleStartTime | string | 定时开始时间，本地时区 `HH:mm` |
| scheduleEndTime | string | 定时结束时间，本地时区 `HH:mm` |
| scheduledBrightness | number | 定时时段内的待机亮度百分比 |

亮度均为 0–100 的整数，`fullBrightness` 应大于 0，且不低于普通和定时待机亮度。定时时段为左闭右开区间，支持跨午夜；开始与结束相同视为无效，表单不得保存。关闭 `scheduleEnabled` 时保留时间配置但不执行。定时策略**只改变自动模式的待机亮度**；感应触发时仍使用 `fullBrightness`。时段边界到达后，在下一次模拟 tick 内更新待机灯具。

### 4.4 告警（Alert）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | string | 告警唯一 ID |
| deviceId | string | 关联设备 ID |
| type | 'offline' \| 'fault' | 本期告警类型；异常触发留待后续迭代 |
| level | 'info' \| 'warning' \| 'critical' | 告警级别 |
| message | string | 告警描述 |
| createdAt | timestamp | 产生时间 |
| resolved | boolean | 是否已处理 |
| resolvedAt | timestamp \| null | 人工处理时间，未处理时为 `null` |

告警处理只表示运营人员确认，绝不改变设备状态；恢复设备也不自动处理历史告警。

总览统计口径：设备总数包含灯具和传感器；在线率为 `status = online` 的设备数 / 设备总数；亮灯率为在线且 `brightness > 0` 的灯具数 / 在线灯具数，分母为 0 时显示 `--`；会话告警数包括已处理和未处理，未处理角标单独计算。默认 `offline` 为 `warning`、`fault` 为 `critical`，具体文案统一从告警生成逻辑提供。

### 4.5 会话能耗明细（DeviceEnergyTotal）

`energyStore.deviceTotals` 以灯具 ID 为键保存以下累计值，用于核对全场汇总；它是会话运行状态，不是持久化业务数据。

| 字段 | 类型 | 说明 |
|---|---|---|
| actualKwh | number | 灯具在在线时段按实际亮度累计的模拟用电量 |
| baselineKwh | number | 灯具仅在在线时段按额定功率常亮的基准电量 |
| onlineDurationMs | number | 灯具参与实际与基准核算的在线累计毫秒数 |
| brightnessWeightedDurationMs | number | 在线时长按亮度百分比加权后的等效满功率毫秒数，用于反推平均亮度 |

逐灯明细之和必须与 `energyStore.actualKwh`、`energyStore.baselineKwh` 一致。平均亮度为 `brightnessWeightedDurationMs / onlineDurationMs × 100%`，在线时长为 0 时显示 `--`。

---

## 5. 核心业务流程

这是系统的可验收行为，Mock 引擎（`mock/simulator.js`）和页面必须遵守同一套状态规则：

1. **感应保持**：模拟引擎按可配置间隔（默认 3 秒）随机触发在线传感器，设 `triggered = true`，并将 `triggeredUntilAt` 设为当前时间加策略的 `triggerHoldSeconds`。保持期内再次触发可延长截止时间；到期后置 `false`。离线或故障传感器立即退出触发状态，不再接收随机触发。
2. **分区联动**：分区内只要至少一个**在线**传感器为 `true`，所有**在线且自动模式**灯具切为 `full`，亮度为 `fullBrightness`。全部在线传感器为 `false` 时，启动一次分区延时回落计时；计时中再次触发则取消计时并保持工作亮度。计时到期仍无触发时，灯具渐变至当前时段的待机亮度。分区无在线传感器时不视为“无车”，不新启动回落计时，灯具保持最近一次自动输出状态直至有有效感应或人工操作。
3. **手动优先**：在线正常灯具可在设备管理和分区监控页手动开关或调光；每次操作将 `manualUntilAt` 设为当前时间加 5 分钟，页面显示“手动模式”和剩余时间。到期自动清除手动模式并立即按感应、倒计时和定时策略重算；“提前恢复自动”按钮执行同样的重算。离线/故障灯具禁用手动按钮；进入离线/故障时清除手动模式。
4. **定时执行**：自动模式待机灯具在定时时段内使用 `scheduledBrightness`，其他时间使用 `standbyBrightness`；全亮中的灯具和手动模式灯具不受定时切换影响。编辑策略并保存后，在下一次模拟 tick 内应用新配置，不需要刷新页面。
5. **故障、恢复与告警**：模拟引擎按文件顶部可配置概率，仅对在线设备注入 `offline` 或 `fault`，同时设置随机恢复时间 `recoverAt` 并产生告警。恢复时间到达后自动变回 `online`；`offline` 与 `fault` 均采用此恢复机制。故障过程内同一设备、同一类型最多生成一条告警；即使先将告警标记已处理，只要设备未恢复，也不再次生成。恢复后结束该故障过程并清除过程锁；若旧告警仍未处理，下一次同类型故障暂不新增，直到旧告警处理完毕。告警的人工处理和设备恢复互不代替。
6. **能耗累计**：从本次登录后的模拟会话启动时计时，按每个灯具的实际状态持续时间累加，不使用随机生成的图表序列。在线灯具模拟功率 `P = ratedPowerW × brightness / 100`，关闭灯具的 `P = 0`；每段电量 `E(kWh) = P(W) × 持续秒数 / 3,600,000`。对比基准为**每个灯具仅在在线时段**按额定功率常亮的电量；离线/故障时段不计入实际电量，也不计入基准，以免把设备停机误算为节能。节能率 `= (基准电量 - 模拟电量) / 基准电量 × 100%`，基准为 0 时显示 `--`。状态变化、策略变化和周期采样时先结算上一段，再开启新段，避免重复累计。会话开始时各曲线从 0 起，刷新页面后设备、策略、告警和能耗均重置为初始状态。

**统一优先级**：离线/故障不可控 ＞ 在线灯具的手动模式 ＞ 在线有效感应及回落计时 ＞ 定时待机亮度 ＞ 普通待机亮度。模拟引擎在应用每次随机事件或策略编辑后，用同一套规则重算受影响分区。首次成功登录后初始化本次模拟会话；引擎只启动一个实例，切换业务页面不重启；退出登录或页面关闭时清理计时器。刷新后若仍有 `sessionStorage` 登录态，则重新初始化模拟会话，不沿用旧运行数据。

---

## 6. 页面清单与验收要点

| 页面 | 路径 | 核心内容 | 验收要点 |
|---|---|---|---|
| Dashboard | `/dashboard` | 全场设备总数、在线率、当前亮灯率、本次会话告警数、会话能耗趋势小图 | 设备和告警数字随 Mock 引擎更新；“本次会话”标签必须明确 |
| ZoneMonitor | `/zones` | SVG 车库平面图，灯具与传感器点位随状态变化，点击点位打开详情 | 灯具亮度有过渡动画；手动操作与设备页同步，异常设备禁用操作 |
| DeviceManage | `/devices` | 设备列表、筛选/搜索、在线状态、手动开关/调光、提前恢复自动 | 手动到期或主动恢复后，两个页面同步显示自动状态 |
| SceneControl | `/scenes` | 策略列表与编辑表单，包含保持、延时、普通/工作亮度、定时起止及定时亮度 | 表单校验有效；保存后下一次 tick 生效；跨午夜时段能正确执行 |
| AlertCenter | `/alerts` | 告警列表，按级别和处理状态筛选，标记已处理 | 新告警角标变化；处理告警不改变设备状态；同一故障过程不重复生成 |
| EnergyReport | `/energy` | 本次会话用电趋势、实际与常亮基准对比、节能率 | 图表从本次会话状态累计而来；显示功率假设与刷新重置口径，数值可复核 |
| Login | `/login` | 已注册账号的用户名密码登录 | 未注册账号或错误密码不能登录；成功后跳转 Dashboard |
| Register | `/register` | 用户名和密码注册表单 | 校验必填、用户名唯一、密码确认；成功后可用新账号登录 |

本期只交付固定模拟数据集，不提供设备、分区、策略的新增/删除，不做用户管理。页面中的“查看”打开可读详情；“编辑”仅用于策略与允许手动操作的在线灯具。页面无数据时应给出明确中文空状态和原因，不得用伪造历史曲线填充。

本地账号仅保存在当前浏览器的 `localStorage`，登录时在本地核对用户名和密码；注册信息刷新后保留。密码不得以明文保存，可用浏览器 Web Crypto 生成随机盐和 SHA-256 摘要后保存盐与摘要；这仍不构成生产级安全认证。登录态用 `sessionStorage` 保存，使同一标签页刷新后保持登录，关闭标签页后重新登录。不得显示内置演示账号。注册用户名应去除首尾空格并做大小写一致的唯一性校验；密码至少 8 位，注册时需二次确认。退出登录清除登录态，不清除已注册账号。生产使用需要真正服务端鉴权。

---

## 7. Mock 引擎设计原则

`mock/simulator.js` 是这个系统的"灵魂"，写的时候要注意：

- 用 `setInterval` 驱动，间隔时间做成可配置常量，不要写死在多处
- 每次 tick 只做「读取当前 store 状态 → 计算变化 → 写回 store」这一件事，不要在组件里散落模拟逻辑
- 所有随机行为（触发感应、注入故障）都要有明确的概率常量，写在文件顶部，方便后续调参
- 组件永远只从 Pinia store 读数据，不要直接引用 `mock/` 下的原始数据，保证数据流单向、可预测
- 恢复时间范围、手动模式时长、趋势采样间隔也集中定义为常量；判断到期使用时间戳，不为每个设备叠加独立定时器
- 提供仅在开发环境可用的手动触发感应、注入故障、推进/缩短等待的验证入口，保证概率事件和五分钟手动到期可以稳定验收；正常业务界面不暴露调试控件
- 第 1 天到第 3 天尚无注册/登录界面，开发环境自动启动模拟会话供联动验证；正式构建只在已有登录态时启动。第 4 天接入注册/登录后，取消开发环境自动启动例外，统一改为成功登录后启动。

---

## 8. 开发协作约定（给 Codex 的执行准则）

1. **先读文档再动手**：每次任务开始前先完整阅读本文档，尤其是第 3 节目录结构和第 4 节数据模型，不要凭空创造新的字段名或目录层级。
2. **改动即更新文档**：如果本次任务新增了数据字段、新增了页面、或调整了业务流程，必须在提交代码的同时更新本文档对应章节。
3. **禁止引入真实后端依赖**：不要添加 axios 请求真实接口、不要引入 WebSocket 客户端连接真实服务器、不要引入需要 API Key 的第三方服务。本地注册与登录不改变此约束。
4. **小步提交**：每完成一个页面或一个 store 模块就是一个可运行的中间状态，不要一次性把所有模块糅在一起改。
5. **命名统一**：变量、字段命名严格按第 4 节数据模型表格来，不要用同义词造成歧义（比如不要一会儿用 `lightStatus` 一会儿用 `lightState`）。
6. **中文界面文案，英文代码命名**：页面上的文字、按钮、提示一律使用中文；变量名、函数名、文件名使用英文。

---

## 9. UI 要求

系统必须具有企业运营管理软件风格。

不同页面必须根据业务使用不同布局。

禁止所有页面统一采用：

“四张卡片 + 表格 + 一个图表”

## 10.页面质量

禁止出现：

- lorem ipsum
- 示例
- demo
- 模板
- 演示账号
- TODO 页面
- 空页面
- 空按钮

所有主要按钮必须有交互。

## 11.操作

本节适用于**实际提供的操作**，不要求为固定 Mock 数据集补齐全套增删改查。

### 新增

本期仅注册账号属于新增，必须进入注册表单；若后续增加其他新建操作，使用表单。

### 编辑

策略编辑必须保存后生效；手动调光必须即时改变设备状态，并能提前恢复自动。

### 删除

本期不提供删除操作；后续增加删除时必须有确认。

### 查看

设备点位和列表查看必须打开详情。

### 筛选

设备和告警筛选必须实际改变结果，并能清除筛选条件。

## 12.开发规则

每次优先修改当前任务相关文件。

不得无原因重构整个项目。

不得删除已完成页面。

## 13.验证

每次开发完成后执行：

`npm run build`

如果失败，必须修复。

最终验收还需手工走通：注册→登录→感应触发及多传感器回落→倒计时中再次触发→手动覆盖及提前/到期恢复→跨午夜定时→故障注入/恢复/去重→告警处理→会话能耗核算→刷新后运行数据重置、账号保留。至少连续运行 10 分钟检查模拟引擎没有重复启动或告警风暴。

## 14.输出

每次任务结束时，用非程序员能理解的语言说明：

1. 本次完成了什么
2. 用户现在可以做什么
3. 我需要手工检查什么
4. 是否存在已知问题
5. build 是否通过

## 15. 待办与迭代记录

> 每次开发任务完成后，在此追加一条记录，方便追溯进度。

### 四个工作日交付顺序

| 工作日 | 必须达到的可运行状态 |
|---|---|
| 第 1 天 | 初始化 Vue 项目、Mock 数据、各业务 store 和单实例模拟引擎；跑通感应保持、联动与回落 |
| 第 2 天 | 完成总览、分区监控和设备管理；跨页面状态同步、手动模式和故障禁用可验收 |
| 第 3 天 | 完成策略编辑与定时执行、告警中心、会话能耗与报表；数值口径可复核 |
| 第 4 天 | 完成注册/登录、界面一致性、边界情况、完整演示流程和构建验证 |

每一天结束时保持项目可运行。若进度受阻，优先保证感应联动、手动控制、定时策略、告警与可追溯能耗的闭环；视觉细节在功能验收之后完善，不用静态数字或假图表代替未完成的功能。

| 日期 | 内容 | 状态 |
|---|---|---|
| 2026-09-14 | 明确感应保持、多传感器联动、手动到期、定时执行、会话能耗、故障恢复与告警去重、本地注册登录，并收敛四天交付范围。 | 仅更新规格，尚未开始编码 |
| 2026-09-14 | 第 1 天：初始化 Vue 3/Vite/Pinia/Router/Element Plus 项目；建立设备、分区、策略 Mock 与六个业务 store；实现单实例模拟引擎、感应保持、多传感器联动和延时回落，并提供开发环境运行视图。 | 8 项场景测试、lint、build 通过；第 2 天继续完成正式业务页面 |
| 2026-09-14 | 改进第一天的联动验证视图：感应器按钮直接标明分区与触发状态，按钮下方实时展示保持剩余时间、灯组亮度及回落倒计时；移除无明显反馈的“结算当前状态”按钮，增加暂停随机事件，方便稳定观察。 | 页面交互已复核；测试、lint、build 通过 |
| 2026-09-14 | 第 2 天第 1 环节：将总览改为按设备、告警和会话能耗实时计算的运营驾驶舱；增加总览、分区、设备导航，并为后两页建立可查看实时数据的入口。 | 浏览器页面交互已复核；测试、lint、格式检查、build 通过；分区平面图和手动控制留待后续环节 |
| 2026-09-14 | 第 2 天第 2 环节：完成 B1/B2 楼层 SVG 平面图、设备点位详情、分区多感应器状态与回落倒计时；在线灯具支持手动开关、调光和提前恢复自动，异常设备禁用控制。 | 浏览器交互及故障禁用已复核；测试、lint、格式检查、build 通过；设备管理页手动控制留待第 3 环节 |
| 2026-09-14 | 第 2 天第 3 环节：完成设备台账、编号或分区搜索、类型与状态筛选、清除条件和设备详情；设备页与分区页共用手动控制组件，增加开发环境缩短手动等待入口以验收到期自动恢复。 | 浏览器复核筛选、跨页手动同步、提前及到期恢复、故障禁用；10 项测试、lint、格式检查、build 通过；跨页面状态同步与专项验收留待第 4 环节 |
| 2026-09-15 | 第 2 天第 4 环节：专项验收总览、分区监控与设备管理的同一会话状态同步；修正分区内手动灯具与自动灯具并存时，总览开发验证反馈误用单盏灯亮度概括全区的问题。 | 跨页操作同步、倒计时延续及到期恢复、异常禁用、总览指标更新、筛选清除、详情与中文空状态已复核；测试、lint、格式检查、build 通过，并连续运行 10 分钟检查单实例引擎 |
| 2026-09-15 | 第 3 天第 1 环节：完成联动策略列表与独立草稿编辑，补齐保持、回落、亮度、定时时段及跨午夜校验；策略保存统一经模拟引擎先结算旧状态能耗，再更新策略并立即重算分区。 | 浏览器复核 50% 工作亮度立即应用、手动灯具不被覆盖、无效相同时刻拦截与跨午夜配置；13 项测试、lint、格式检查、build 通过 |
| 2026-09-15 | 第 3 天第 2 环节：完成告警中心、级别与处理状态筛选、设备实时状态关联和人工处理；增加仅开发环境可用的恢复等待缩短入口，设备仍由正常模拟 tick 自动恢复。 | 浏览器复核离线告警、正常 tick 自动恢复、恢复后告警仍未处理、处理不改变设备状态、筛选清除与导航角标；15 项测试、lint、格式检查、build 通过 |
| 2026-09-15 | 第 3 天第 3 环节：扩展会话能耗为逐灯可核对账本，集中配置趋势采样间隔；新增能耗报表、真实累计趋势、计算口径和逐灯公式核算。 | 浏览器复核 50% 分段累计、逐灯公式、分区筛选、真实采样与明细合计；19 项测试、lint、格式检查、build 通过 |
| 2026-09-15 | 第 3 天第 4 环节：整体验收第三天策略、告警、能耗功能，并回归前两天的分区监控、设备筛选、手动控制与恢复；增加跨完整状态链的能耗连续性测试。 | 浏览器完成跨页同步、刷新重置和告警独立性回归；连续运行 10 分 7 秒无重复启动或告警风暴；20 项测试、lint、格式检查、build 通过 |
